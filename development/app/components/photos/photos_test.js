'use strict';

describe('geoto.photos module', function() {

  beforeEach(module('geoto.photos'));

  describe('photos controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view1Ctrl = $controller('GalleryCtrl');
      expect(view1Ctrl).toBeDefined();
    }));

  });
});